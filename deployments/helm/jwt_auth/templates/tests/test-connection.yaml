apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jwt_auth.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "jwt_auth.name" . }}
    helm.sh/chart: {{ include "jwt_auth.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jwt_auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
